# ============================================================
#   GtkColumnViewCell
# ============================================================

# See GtkListItem.toml: the GIR's gpointer return would otherwise yield a
# System.Address rather than a GObject.

[[method]]
id = "gtk_column_view_cell_get_item"
return = "GObject*"

[method.doc]
extend = true
text = "@return The item displayed"
