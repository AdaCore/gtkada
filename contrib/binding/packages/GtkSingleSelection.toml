# ============================================================
#   GtkSingleSelection
# ============================================================

# See GtkListItem.toml: the GIR's gpointer return would otherwise yield a
# System.Address rather than a GObject.

[[method]]
id = "gtk_single_selection_get_selected_item"
return = "GObject*"

[method.doc]
extend = true
text = "@return The selected item"
