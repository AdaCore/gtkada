# ============================================================
#   GVariantIter
# ============================================================
into = "GVariant"
ada = "Glib.Variant"

[[method]]
id = "g_variant_iter_copy"
bind = false
# results in duplicate import

[[method]]
id = "g_variant_iter_free"
bind = false
# results in duplicate import

[[method]]
id = "g_variant_iter_n_children"
bind = false
# duplicate import