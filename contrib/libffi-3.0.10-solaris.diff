*** configure.orig	Wed Nov  9 18:09:58 2011
--- configure	Wed Nov  9 18:21:32 2011
***************
*** 1,4 ****
--- 1,8 ----
  #! /bin/sh
+ 
+ mkdir -p sparc-sun-solaris2.8/
+ cp install-sh sparc-sun-solaris2.8/
+ 
  # Guess values for system-dependent variables and create Makefiles.
  # Generated by GNU Autoconf 2.68 for libffi 3.0.10.
  #
***************
*** 16338,16344 ****
  	@ HOST="\$(HOST)\" \\\\\\
  	; test ".\$\$HOST" = "." && HOST=$x sh $ax_enable_builddir_auxdir/config.guess $x \\\\\\
  	; BUILD=$x grep "^#### \$\$HOST " Makefile | sed -e 's/.*|//' $x \\\\\\
! 	; use=$x basename "\$\@" -all $x; n=$x echo \$\$BUILD | wc -w $x \\\\\\
  	; echo "MAKE \$\$HOST : \$\$n * \$\@"; if test "\$\$n" = "0" ; then : \\\\\\
  	; BUILD=$x grep "^####.*|" Makefile |tail -1| sed -e 's/.*|//' $x ; fi \\\\\\
  	; test ".\$\$BUILD" = "." && BUILD="." \\\\\\
--- 16342,16348 ----
  	@ HOST="\$(HOST)\" \\\\\\
  	; test ".\$\$HOST" = "." && HOST=$x sh $ax_enable_builddir_auxdir/config.guess $x \\\\\\
  	; BUILD=$x grep "^#### \$\$HOST " Makefile | sed -e 's/.*|//' $x \\\\\\
! 	; use=$x basename "\$\@" -all $x; n=$x echo \$\$BUILD | wc -w | tr -d ' ' $x \\\\\\
  	; echo "MAKE \$\$HOST : \$\$n * \$\@"; if test "\$\$n" = "0" ; then : \\\\\\
  	; BUILD=$x grep "^####.*|" Makefile |tail -1| sed -e 's/.*|//' $x ; fi \\\\\\
  	; test ".\$\$BUILD" = "." && BUILD="." \\\\\\
***************
*** 16371,16377 ****
  	@ HOST="\$(HOST)\" \\\\\\
  	; test ".\$\$HOST" = "." && HOST=$x sh $ax_enable_builddir_auxdir/config.guess $x \\\\\\
  	; BUILD=$x grep "^#### .*|" Makefile | sed -e 's/.*|//' $x \\\\\\
! 	; use=$x basename "\$\@" -all $x; n=$x echo \$\$BUILD | wc -w $x \\\\\\
  	; echo "MAKE \$\$HOST : \$\$n * \$\@ (all local builds)" \\\\\\
  	; test ".\$\$BUILD" = "." && BUILD="." \\\\\\
  	; for i in \$\$BUILD ; do test ".\$\$i" = "." && continue \\\\\\
--- 16375,16381 ----
  	@ HOST="\$(HOST)\" \\\\\\
  	; test ".\$\$HOST" = "." && HOST=$x sh $ax_enable_builddir_auxdir/config.guess $x \\\\\\
  	; BUILD=$x grep "^#### .*|" Makefile | sed -e 's/.*|//' $x \\\\\\
! 	; use=$x basename "\$\@" -all $x; n=$x echo \$\$BUILD | wc -w $x  | tr -d ' ' \\\\\\
  	; echo "MAKE \$\$HOST : \$\$n * \$\@ (all local builds)" \\\\\\
  	; test ".\$\$BUILD" = "." && BUILD="." \\\\\\
  	; for i in \$\$BUILD ; do test ".\$\$i" = "." && continue \\\\\\
