SHELL = /bin/sh
@SET_MAKE@

.SUFFIXES:
.SUFFIXES: .c .o .ads .adb .ali

.PHONY:
.PHONY: clean-generic mostlyclean-generic \
	clean distclean mostlyclean maintainer-clean \
	force

target  = testgtk
src_dir = ../src

CC = @CC@
GTKADA_CONFIG=gtkada-config
GNATFLAGS = @CFLAGS@ -gnata
GNATFLAGS_DEVEL = @CFLAGS@ -g -gnatawu

GNATMAKE = gnatmake
GNATMAKELDFLAGS = -largs -L$(src_dir) -lgtkada @GTK_LIBS@

CP = cp -f

## This variable should be set to "True" if the opengl directories
## have been installed on the system.
HAVE_OPENGL=@HAVE_OPENGL@

ifeq ($(HAVE_OPENGL),True)
OPENGL_C_OBJECTS=lw.o
OPENGL_LIBS= @GL_LIBS@
else
OPENGL_C_OBJECTS=
OPENGL_LIBS=
endif

## This variable should be set to True if the libart directories
## have been installed on the system.

HAVE_LIBART=@HAVE_PIXBUF@

all: opengl/view_gl.adb pixbuf/libart_demo.adb $(OPENGL_C_OBJECTS) $(target) 

$(target)::
	@echo "-------------------------------------"
	@echo $(GNATMAKE) $(GNATFLAGS) $(target) '`'$(GTKADA_CONFIG)'`'
	@echo "-------------------------------------"
	@if [ -f $(src_dir)/.devel ]; then \
	   $(GNATMAKE) $(GNATFLAGS_DEVEL) $(target) -I$(src_dir) -I$(src_dir)/opengl -Iopengl -I$(src_dir)/pixbuf -Ipixbuf -aO$(src_dir)/lib-obj -largs $(GNATMAKELDFLAGS) $(OPENGL_C_OBJECTS) $(OPENGL_LIBS) @PIXBUF_LIBS@; \
	else \
	   $(GNATMAKE) $(GNATFLAGS) $(target) -I$(src_dir) -I$(src_dir)/opengl -Iopengl -I$(src_dir)/pixbuf -Ipixbuf -largs $(GNATMAKELDFLAGS) $(OPENGL_C_OBJECTS) $(OPENGL_LIBS) @PIXBUF_LIBS@; \
	fi

opengl/view_gl.adb:: opengl/view_gl.gpb Makefile
	gnatprep -r -c -DHAVE_GL=$(HAVE_OPENGL) -DWIN32=False \
	  opengl/view_gl.gpb opengl/view_gl.adb

pixbuf/libart_demo.adb:: pixbuf/libart_demo.gpb Makefile
	gnatprep -r -c -DHAVE_LIBART=$(HAVE_LIBART) -DWIN32=False \
	  pixbuf/libart_demo.gpb pixbuf/libart_demo.adb

lw.o: opengl/lw.c opengl/lw.h
	$(CC) -c opengl/lw.c -Iopengl @GTK_CFLAGS@

install: all
	@echo "Nothing to do --  just run the ./testgtk program"

clean:
	-${RM} *.o *.ali *~ testgtk b_testgtk.c b~testgtk.ad? core
	-${RM} opengl/view_gl.adb
	-${RM} .\#*

distclean: clean
	-${RM} Makefile

mostlyclean: clean
	-${RM} Makefile

maintainer-clean: clean
	-${RM} Makefile

force:

