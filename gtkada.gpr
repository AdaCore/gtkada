project Gtkada is

   for Source_Dirs use ("src", "src/glade", "src/gnome", "src/opengl");
   for Object_Dir use "src/lib-obj";

   package Compiler is
      for Default_Switches ("Ada") use
        ("-g", "-O2", "-gnaty", "-gnatwu", "-gnatwe");
   end Compiler;

end Gtkada;
